"""Single source of truth for the Oztudy release version (SemVer).

Bump this on every release; setup.py, run_desktop.py, the /api/version
endpoint and the Homebrew formula all read from here.
"""
__version__ = "1.0.1"
